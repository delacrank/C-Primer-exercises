#include <iostream>
    
using namespace std;
    
void swap(int&, int&);

int main()
{
    int x = 5, y = 10;
    swap(x, y);
    
    cout << x << " " << y << endl;
}

void swap(int &x, int &y)
{
    int temp = 0;
    temp = x;
    x = y;
    y = temp;
}
