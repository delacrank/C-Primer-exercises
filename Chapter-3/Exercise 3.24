#include <iostream>
#include <vector>
#include <string>
    
using namespace std;
    
int main()
{
    vector<int> v1;
    int i = 0;
    int j = 0;
    
    while(cin >> i)
        v1.push_back(i);
        
    for(auto it = v1.begin(); it != v1.end(); ++it){
        int sum = *it + (*it + 1);
        cout << sum << endl;
    }
        
    


    return 0;
}
