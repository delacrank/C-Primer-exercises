#include <iostream>
#include <vector>
#include <string>

using namespace std;
    
int main() {
   
    string s, r;
    int i;
    
    while(cin >> s) 
    {
        if(r == s)
        {
            cout << "duplicate " << r <<  endl;
            s = '\0';
            break;
        }
        else
            r = s;
    }
}
