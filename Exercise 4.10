#include <iostream>
#include <vector>
#include <string>
    
using namespace std;
    
int main() {
        
    int i = 0;
    while(cin >> i) {
        cout << i << endl;
            if(i >= 42)
                return 0;
    }
}
