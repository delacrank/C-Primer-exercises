#include <iostream>
#include <vector>
#include <string>

using namespace std;
    
int main() {
   
    string s, t;
    char c;
    
    do  {    
        cout << "Enter two strings" << endl;
        cin >> s >> t;
        if(s.size() > t.size())
            cout << "string one is greater than string two" << endl;
        else if(s.size() == t.size())
            cout << "string one and string one are equal lenth" << endl;
        else
            cout << "string two is greater than string one" << endl;
        
        cout << "To continue press y" << endl;
        cin >> c;
    }
    while(c == 'y');
}
